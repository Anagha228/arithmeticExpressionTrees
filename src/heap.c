#include <stdio.h>
#include <string.h>
#include <strings.h>

int main (int argc, char * argv[]) {
    FILE *fptr = fopen("f.dat", "r");
    int fArray [20][10];
    
    if (fptr == NULL) {
        printf("Could not open file %s\n", fileName);
        return -1; // Error
    } else {

        while ((!feof (fileHandle)) && (noOfRows <= 20)){
            
        }

    }

    return 0;
}

